## Packages
date-fns | Date formatting and manipulation
animejs | For complex animations and page transitions
lucide-react | Icon set (already in base but ensuring version)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
